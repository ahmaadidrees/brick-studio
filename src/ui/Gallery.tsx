/**
 * Development-only component gallery, served at /dev/ui (see routes.ts).
 * Placeholder: the brand foundation lane replaces this file with the real
 * primitive review page. Keep the default export.
 */
export default function Gallery() {
  return (
    <main style={{ padding: '2rem', maxWidth: 960, margin: 'auto' }}>
      <h1>UI gallery</h1>
      <p>Development-only review page for shared UI primitives.</p>
    </main>
  )
}
