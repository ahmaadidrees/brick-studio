# W5 — Scene & plate sheet, custom bricks, resize, color

Worktree `/Users/ahmaadidrees/.codex/worktrees/brand-w5`, branch `claude/brand-w5`, base `3fc41bd`.
Node 22. Owned files only: `src/brick/contentPicker/**`, `customParts/{CreateBrickSheet,ResizeBrickSheet,CreateBrickPreview}.tsx(+tests)`,
`create-brick-sheet.css`, `CustomColorPicker.tsx(+test)`, `custom-color-picker.css`.

## Done

- Phase A slice 1 — scene & plate sheet (board 08): "Choose your scene" heading + lead, segmented 64/96/128 plate choice
  with "Your creation stays centered.", current size, shrink warning, locked explanation, in-sheet shrink rejection
  (real `resizeBuildPlate` message, Keep current size / Back to building), "Preview — only you can see this" eyebrow
  while the draft differs from the committed selection, plate in the footer summary, selected badge on the art.
  Shared `contentPicker/sheetFocus.ts` (focus/restore, Tab trap, Escape).

## In progress

- Custom bricks (board 10), color picker (board 10), mobile (board 16), scene art refinement.

## Blocked

Nothing.

## Requests for lead

- **W4 (BrickStudioApp `applyContentSelection`)**: return the brick-core rejection so the sheet can show it inline instead
  of only a toast. `WorldAndCharacterSheet.onApply` now accepts a return value `void | boolean | { ok: false; message }`
  (props unchanged; return type widened, existing void callbacks still type-check). Suggested change:
  `if (!resized.ok) { useBrickStore.setState({ toast: resized.error.message }); return { ok: false, message: resized.error.message } }`
  Until then the app keeps its current toast behaviour and the sheet simply stays open.
- **W7**: scene thumbnails `public/brand/media/scene-{classic,toy-room,brick-valley,sky-island}-400.{avif,webp,png}` (400×220,
  captured runtime compositions, Classic Studio neutral). `PreviewArtwork` will layer them over the SVG art with an
  `onError` fallback once the manifest lists them; until then the SVG art is the thumbnail.

## Evidence

- `docs/brand/qa/w5/capture-boards.mjs` — Playwright capture of boards 08/10 at 1366×768, 390×844, 320×740 (asserts no
  horizontal overflow and that every open dialog fits the viewport).
- `docs/brand/qa/w5/baseline/` — before; `docs/brand/qa/w5/final/` — after (written at the end of the pass).
