# W8 — Independent QA and evidence

Worktree `/Users/ahmaadidrees/.codex/worktrees/brand-w8`, branch `claude/brand-w8`, base `f8c7ad3`; since `deb0f24` the
branch carries the merged W0+W1 foundation (`7d9a48b`). Owns `scripts/qa/**`, `docs/brand/qa/**`. No product files touched.

## Done

- Harness parameterization (`scripts/qa/lib/env.mjs`): `PLAYWRIGHT_MODULE`, `CHROME_PATH`, `UI_ORIGIN`/`QA_ORIGIN`/`QA_API`,
  `UI_OUTPUT`/`QA_OUTPUT`; perf harness localhost guard; multiplayer socket check accepts `ws://` for an `http://` API.
- `scripts/qa/lib/ui.mjs`: shared locator loader, step runner (`click`, `fill`, `scrollToHeading`, `loseContext`, …),
  guest/session storage seeding, studio-menu import/export. `brand-surfaces.mjs`, `board-captures.mjs` and
  `schema-roundtrip.mjs` use it.
- `scripts/qa/brand-surfaces.mjs` + `locators.json`: 24 surfaces × six viewports + 200 % zoom + reduced motion.
  `classroom=join|signin|teacher` are active surfaces asserting `aria-pressed` plus the mode-only field; scrollable
  regions are recognised on both axes; `/live/<unknown>` accepts the blocked heading or the classroom panel.
  Locators carry the CONTRACTS.md header/menu names (`brandHome` "Brickgineers", `people`, `share`, `room`,
  `exploreMode` regex `^Explore( mode)?$`, `saveStatus` role=status, `menuExport`, …).
- `scripts/qa/board-captures.mjs`: all 16 boards (31 states) at 1366×768 and 390×844 → `docs/brand/qa/integrated/boards/`
  by default, `captures.json` + `README.md` table, per-capture header probe, `needs-fixture` with the exact reason for
  04-reset/05/13/14, board 11 creates a real guest room when the worker is reachable.
- `scripts/qa/schema-roundtrip.mjs` + fixtures `expanded-128.brickstudio.json` (schema 3) and `legacy-v1.brickstudio.json`:
  import → export equality after import, reload and Home → Continue → reload.
- `scripts/qa/route-transfer.mjs`: per-route compressed transfer against `vite preview`.
- Baseline complete except performance (`docs/brand/qa/baseline/RESULTS.md`, exact base SHA per artifact): refinement UI
  7/7, character customizer 7/7, bundle + transfer 5/5 routes (all `f8c7ad3`); surfaces matrix 264 runs 231/33 with the
  pre-brand defect list D1–D10 and owner lanes, local two-client guest room 9/9 on `wrangler dev`, schema round trip 3/3,
  board captures 51 captured / 10 need a fixture / 0 failed (all `4002fc7` = `f8c7ad3` + W0 + W1 foundation).
- `docs/brand/qa/README.md`: integrated-candidate run book (ten ordered steps with exact commands, ports, gates) and
  every harness's variables; `docs/brand/qa/ACCEPTANCE-MATRIX.md`: board-by-board reach path, assertion, harness, owner,
  fixture.
- Verification for this lane's commits: `npx tsc -p tsconfig.app.json --noEmit` clean; full `npx vitest run` 104 files /
  957 tests passed (no vitest files are owned by W8; the QA scripts are `node --check`ed and were executed as listed in
  `RESULTS.md`).

## In progress

- Nothing. The next W8 action is the integrated run itself (README run book), once the lead names the candidate.

## Blocked

- Performance baseline (`verify-expanded-performance.mjs`) deliberately not run: the host carried seven other lanes
  (load average 5–10 on 18 cores). Placeholder row in `RESULTS.md` with the exact invocation; needs a quiet host.
- Boards 05, 13, 14 and the 04 password-reset state cannot be captured locally: they need a classroom/teacher session
  (`sessionStorage` `brick-studio.classroom-session.v1`) and a worker with the classroom secrets; `wrangler dev` here has
  no `.dev.vars`. `board-captures.mjs` accepts `SESSION_STORAGE_FILE` when such a fixture exists.

## Requests for lead

- Port 5198 (W8's assigned Vite port) was occupied by a Codex-spawned `python -m http.server 5198` (pid 29339) when the
  baseline started; 5210 likewise. W8 used 5208/5209/5211. If ports are allocated to lanes, Codex's static servers should
  stay off the 519x/521x range, or lanes should always pass `UI_ORIGIN`.
- `[::1]:8787` is held by a Node server from the `portalblaster-claude-gate-a` worktree while W8's `wrangler dev` holds
  `127.0.0.1:8787`; the run book therefore uses `127.0.0.1`, never `localhost`, for the worker.
- W4: if the world title menu trigger's accessible name changes from "World menu", update `worldMenu` in
  `scripts/qa/locators.json` (one JSON edit) — every harness keys off it.
- Pre-brand defects D1–D10 in `docs/brand/qa/baseline/RESULTS.md` are assigned to W1/W3/W4/W5/W6; none needs a W8 change.
  The strict matrix (`STRICT_TOUCH_TARGETS=1 STRICT_FOCUS=1`) will fail on D6/D7 until the owning lanes fix them.
- For boards 05/13/14 evidence, provide a session fixture JSON (or a staging pair) and W8 will run
  `SESSION_STORAGE_FILE=… node scripts/qa/board-captures.mjs BOARDS=04,05,13,14`.

## Requests for Codex

- None. The local worker (`wrangler dev`, no secrets) serves guest rooms, so no hosted configuration is needed for the
  guest two-client procedure.

## Evidence paths

- `docs/brand/qa/README.md` — run book and invocations.
- `docs/brand/qa/ACCEPTANCE-MATRIX.md` — board/extra-coverage matrix.
- `docs/brand/qa/baseline/RESULTS.md` — per-artifact base SHA, counts, defects D1–D10.
- `docs/brand/qa/baseline/{refinement-ui,character-customizer,route-transfer,surfaces,multiplayer-local,schema-roundtrip,boards}/`
  (16 MB total; surfaces and boards screenshots are JPEG, one per 1366×768/390×844 default run plus every failed run).
- Scratch outputs (not in the repo): full-matrix PNG/JPEG set, schema exports and board PNGs under the session scratchpad.
