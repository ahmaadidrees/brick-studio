# W1 status — Worker, migration, client

Branch `claude/flows-w1` from `claude/flows-v2` at `3e0a609`.

## Commits

- `bd20397` — `src/classroom/contracts.ts` (new world/class fields, `ClassroomClientSurface`), `src/classroom/mockClient.ts`
  (`createMockClient({ as: 'student' | 'teacher' | 'guest' })`), mock coverage in `src/classroom/client.test.ts`.
  **Cherry-pick this one to the other lanes.**

## Mock client (for W3/W4/W5)

`import { createMockClient, MOCK_IDS, MOCK_CLASS_CODE, MOCK_PASSWORD } from 'src/classroom/mockClient'`.
The mock implements `ClassroomClientSurface` (contracts.ts): `getSession/subscribe/setSession`, `request(path, method, body)`
with the Worker's routes, `login/register/authenticate/classRoster/resolveClass/changePassword/startGoogleTeacher/signOut`,
`me/listWorlds/listClasses/listStudents/getWorld/createWorld/saveWorld/renameWorld/duplicateWorld/listCheckpoints/restoreWorld`,
`setWorldSharing/setWorldHidden/copyWorld/createClass/updateClass/updateStudent`. Errors are `ClassroomError` from
`client.ts` with the server codes (`username_taken` + `details.suggestions`, `sharing_disabled`, `world_limit`, `read_only`,
`world_hidden`, `class_closed`, `revision_conflict`). `mock.reset(role)` restores the fixture. Student password is
`brick-time`; teacher password `teach-bricks` (any email); class code `MAKERS3`.

## Requests for the lead

1. The new `ClassroomWorld` fields are required (per contract), so five test fixtures outside W1's ownership no longer
   typecheck (`npx tsc -p tsconfig.app.json --noEmit`). Each needs the same additions on its world/class literals:
   worlds: `visibility: 'private', canEdit: true, ownerName: 'Alex', sharedAt: null`; classes: `studentsCanShare: true`.
   - `src/brick/studioNavigation.test.tsx:56` (world)
   - `src/classroom/ClassInvite.test.tsx:7,14,20,34` (class)
   - `src/classroom/ClassroomPanel.test.tsx:9` (class), `:11` (world factory default)
   - `src/classroom/cloudAutosave.test.ts:8` (world)
   - `src/classroom/useClassroomWorld.test.tsx:16` (world)
   Runtime tests still pass; only the type literals are incomplete.
