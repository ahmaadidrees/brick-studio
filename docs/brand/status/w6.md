# W6 — Character studio and original avatars

Worktree `/Users/ahmaadidrees/.codex/worktrees/brand-w6`, branch `claude/brand-w6`, base `3fc41bd`, merged W0+W1 foundation
(`7d9a48b`) at `15ac2d2`. Node 22. Vite on port 5196. Browser scripts and screenshots live under `docs/brand/qa/w6/`.

## Done

- **Board 09 character studio** (`src/brick/characters/CharacterStudio.tsx`, props unchanged per CONTRACTS): intro, one
  live rotatable preview (`CharacterPreview`: single Canvas, DPR `[1, 1.5]`, `frameloop="demand"` when paused, scrolled
  out, tab hidden or under reduced motion; Idle/Walk/Run/Jump/Pause; "Drag to turn" via OrbitControls; boundary message
  when the 3D context cannot be created; renderer and materials disposed on unmount), character cards through
  ContentPicker (W5), Toy Figure categories with real per-category **Keep** locks, per-slot colors with locks
  (`PaletteControls`), coordinated color sets (`LookColors`), **Mix it up** (`studioMix.ts`: one complete draft, honors
  every lock, never touches skin/hair color, disabled honestly when everything is kept), wardrobe (`WardrobePanel`,
  `brick-studio.wardrobe.v1`, max 24, favorites, confirm-before-remove, failed save and blocked storage surfaced as an
  error status, "Saved on this device" note). Apply/Cancel stay with the sheet; the studio only calls `onDraftChange`.
- **Shared primitives and tokens**: every studio button/field is `src/ui` `Button`/`Field`; `character-studio.css` and
  `character-preview.css` use only the `:root` semantic tokens (no legacy `--studio-*` definitions; `appearance-controls.css`
  removed).
- **Avatar polish (runtime)**: Classic Builder defaults coral/cornflower/butter/ink (`BlockAvatar`, geometry unchanged,
  overrides still win); Toy Figure defaults on the palette and hands take the skin material (mittens in the chosen skin
  tone, no yellow claws); Robot Hero repaint defaults coral limbs / warm-white shell / ink visor+boots, binary untouched.
- **Avatar polish (Blender)**: `scripts/art/build_characters.py` maps brand hex → linear base color; Sunshine→butter,
  Lagoon→cornflower, Clay→coral, Porcelain→warm white, Ink→brand ink, Pink→soft coral-pink, new static `Coral` for Pip's
  boots. Rebuilt headless with Blender 5.2 (14 s): `assets/characters-original/{pip,fern,nova}.glb`, `*-preview.png`
  (640×640), `contact-sheet.png`, `original-characters.blend`, `verification.json`, README table. Geometry identical
  (same triangles/meshes/bounds, height 1, soles at ground); Pip material count 5→6. Material names unchanged so runtime
  palette channels resolve; IDs, serialization, scale, pivots, grounding, collision and animation contracts unchanged.
- **Verification**: `npx tsc -p tsconfig.app.json --noEmit` clean; `npx vitest run src/brick/characters` 13 files /
  111 tests; full `npx vitest run` 108 files / 985 tests. Browser probes (headless Chrome, port 5196): paused preview
  renders no new frames, drag turns the figure and it stays still after; reduced-motion context shows the note, no
  Pause, no animation; Cancel discards a draft (nothing persisted, reopen shows the committed character); Apply commits;
  sheet footer visible at 1366×768, 390×844 and 320×740 with the character tab open; no page errors.
- **W8 D7 touch targets (lead follow-up)**: on coarse pointers every control under `src/brick/characters/**` is at least
  44×44 — `@media (pointer: coarse)` in `character-studio.css` / `character-preview.css` raises the sm primitives, Keep
  locks, choice chips, icon buttons, swatches (the native color input now covers its whole 44px swatch) and the preview
  action pills; the saved-outfit look button keeps a 160px flex basis so the icon/confirm buttons wrap under it on 320px
  phones instead of squeezing it. Names and `aria-pressed` unchanged. Measured with Playwright mobile emulation
  (`pointer: coarse` true) over all 96 controls in the character tab with an outfit saved and its Remove confirmation
  open: **390×844** preview pills 64×44, locks 75×44, nothing under 44; **320×740** pills 87–132×44, locks 75×44, nothing
  under 44; **844×390** pills 80×44, locks 75×44, nothing under 44. No page errors.
- Wave-0 audit (below) retained as the baseline.

## In progress

Nothing.

## Blocked

Nothing.

## Deliberate deviations from board 09

- Display names stay "Toy Figure" and "Classic Builder" (lead decision; harness locators depend on them). Board's
  "Blocky Builder" label not used.
- Idle/Walk/Run/Jump and the figure choice chips are toggle buttons (`button[aria-pressed]`), not `SegmentedControl`:
  `scripts/qa/verify-character-customizer.mjs` drives them by button name + `aria-pressed` and clicks the saved-outfit
  look button by its exact name. They are styled as pills/chips with the primitives' tokens.
- Board's "Face/Hair/Outfit" tile rows are text chips (real option labels; no LEGO-style thumbnails). Board's per-row "›"
  drill-down does not exist; all options are inline.
- Robot Hero stays selectable; it is excluded from marketing media (lead decision).
- The preview is the live 3D adapter, not a rendered still; cards keep W5's artwork.

## Requests for lead

- **W5** (`src/brick/contentPicker/ContentPicker.tsx`): no change needed. Character cards keep their radio roles/names;
  W6 renders palette swatches inside CharacterStudio (no `onPaletteChange` passed). At 390×844 the picker's one-card-per-
  row layout makes the character list tall (≈4 screens before the figure controls); a two-up grid on phones would shorten
  the studio — W5's call.
- **W7**: character marketing sources are `assets/characters-original/*-preview.png` (640×640, regenerated on the brand
  palette; Pip/Fern/Nova) and, for Toy Figure, a runtime capture from `docs/brand/qa/w6/after-toy-figure-idle.png` or the
  live preview. Robot Hero is excluded from marketing.
- **W8**: no locator changes. New stable hooks if useful: `.character-preview[data-animating]` (present only while frames
  are rendering), `region "Character colors"`, `region "Coordinated colors"`, `button "Mix it up"`, `button "Keep <part>
  when mixing"` (aria-pressed), `group "<Body|Face|Hair & hats|Outfit|Accessory|Skin tone|Hair color|slot label>"`.
- **Lead / W1 (optional)**: a `Button` `pressed` prop (styled `aria-pressed` state) would let the studio drop its two local
  `[aria-pressed='true']` rules in `character-studio.css` / `character-preview.css`.

## Evidence paths

- `docs/brand/qa/w6/README.md` — script and what was checked.
- `docs/brand/qa/w6/after-<id>-{idle,walk}.png` for `classic`, `toy-figure`, `cc0-hero`, `pip`, `fern`, `nova`.
- `docs/brand/qa/w6/after-sheet-{1366x768,390x844,320x740}.png` — board 09 desktop and phone widths, footer visible.
- `docs/brand/qa/w6/audit/before-*` — wave-0 baseline.
- `assets/characters-original/{contact-sheet,pip-preview,fern-preview,nova-preview}.png` — Blender renders.

## Wave-0 audit (before any restyle)

Captured with `docs/brand/qa/w6/audit-avatars.mjs` (real WebGL preview, Chrome headless, 1366×768):
`docs/brand/qa/w6/audit/before-<id>-{idle,walk}.png` for all six IDs plus `before-sheet-{1366x768,390x844,320x740}.png`.

Brand rule checked: no LEGO minifigure look — no round yellow toy heads, no C-shaped claw hands, no tapered
minifigure torso. Direction I coherence: cornflower `#5888DA`, coral `#F17861`, butter `#F3CA74`, ink `#263C51`,
warm white `#F8F4EB`; clean toy-plastic materials; original characters.

| ID | Rule verdict | Notes (before) | Bounded polish (done; IDs, pose, pivots, scale, serialization kept) |
|---|---|---|---|
| `classic` Classic Builder (`BlockAvatar`) | Passes | Box head in a tan skin tone, cube hands, straight box torso. Default colors were near but off palette. | Defaults on the brand palette: torso coral, pants cornflower, badge butter, shoes ink. No geometry change. `color`/`palette` overrides keep winning. |
| `toy-figure` Toy Figure | Passes, closest call | Peach ellipsoid head with a cap; torso pinches at the waist; hands were spheres in the yellow accent material, reading as "yellow toy hands". | Hands take the skin material; accent stays on collar, cuffs, brim, emblem. Defaults coral / cornflower / butter / ink. Geometry, joints, face atlas, appearance options unchanged. |
| `cc0-hero` Robot Hero | Passes | Vendored CC0 RobotExpressive, repainted at runtime. | Repaint defaults coral limbs, warm-white shell, ink visor/boots. No asset change (byte-for-byte provenance kept). |
| `pip` | Passes | Original. Helmet gold, panels teal (off palette), body mint-white, cyan glow. | Blender: Sunshine→butter, Lagoon→cornflower, Porcelain→warm white, Ink→brand ink; static Coral boots. |
| `fern` | Passes | Original. Sage coat, clay cap, gold scarf, bark boots. | Clay→coral, Sunshine→butter, Ink→brand ink. Moss/Cream/Bark kept. |
| `nova` | Passes | Original. Lavender body, pink belly, cyan ear tips, gold star pack. | Pink→soft coral-pink, Sunshine→butter, Ink→brand ink. Orchid/Signal kept. |

Lead decisions received: keep the display names; Robot Hero stays in the product but out of marketing; Toy Figure default
skin tone stays as brick-core defines it (`#f0bd86`).
