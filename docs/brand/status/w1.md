# W1 — Brand foundation and design system

Branch `claude/brand-w1` (base `f8c7ad3`). Dev port 5191.

## Done

- `src/brand/brand.ts`: `BRAND_NAME`, `BRAND_TAGLINE`, `BRAND_DESCRIPTION`, `BRAND_SHORT_DESCRIPTION`, `BRAND_PALETTE`,
  `BRAND_PRIMARY_STRONG`, `BRAND_MARK_COLORS`, font stacks. No domain strings.
- `src/brand/BrickMark.tsx` (`BrickMark`, `MARK`, `lobePath`) and `src/brand/Wordmark.tsx` (`Wordmark`, `BrandLockup`), `brand.css`.
- `src/styles.css`: semantic tokens per CONTRACTS.md plus `--studio-*`, `--landing-*`, `--live-*`, `--content-picker-*`,
  `--classroom-accent` aliases; global focus ring, reduced-motion kill switch, base resets. Dead Rover Island rules removed.
- `src/ui/`: `Button`, `Field`/`TextField`, `Sheet`/`Dialog`, `SegmentedControl`, `SaveStatus` (+ `describeSaveStatus`),
  `Gallery`, `ui.css`, `gallery.css`, tests for each.
- `index.html`: Google Fonts preconnect hints; `src/test/indexHtml.test.ts` allows them.

## In progress

- Identity assets (favicon/icons/OG/manifest/index.html copy).
- Gallery screenshots and contrast table under `docs/brand/qa/w1/`.

## Blocked

- Nothing.

## Requests for lead

1. Wire the dev-only `/dev/ui` route to `src/ui/Gallery.tsx` (default export and named `Gallery`).
2. The lane stylesheets still define `--studio-*` (`.brick-studio`), `--landing-*` (`.brick-landing`), `--live-*`
   (`.live-world-page`), `--content-picker-*` and `--classroom-accent` on their own root classes, which override the
   `:root` aliases. Ask W2–W5 to delete those local definitions (or migrate to the semantic names) so the palette lands.
3. Global `h1/h2/h3` now use `--font-display` (Fredoka) and every element gets a `:focus-visible` ring from `:root`.
   Lanes that set their own outline should keep it ≥ 3px and visible.
4. Fredoka is not installed on the build host, so the OG card stays text-free (mark + vector toy room). If a wordmark on
   the card is wanted, we need an OFL Fredoka WOFF2/TTF checked in (self-hosting decision) before rasterizing.

## Evidence

- `npm test`: 103 files / 929 tests green; `npx tsc -p tsconfig.app.json --noEmit` clean (foundation commit).
