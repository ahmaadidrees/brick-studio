# W8 — Independent QA and evidence

Worktree `/Users/ahmaadidrees/.codex/worktrees/brand-w8`, branch `claude/brand-w8`, base `f8c7ad3`.
Owns `scripts/qa/**`, `docs/brand/qa/**`. No product files touched.

## Done

- Harness parameterization (`scripts/qa/lib/env.mjs`): `PLAYWRIGHT_MODULE`, `CHROME_PATH`, `UI_ORIGIN`/`QA_ORIGIN`/`QA_API`,
  `UI_OUTPUT`/`QA_OUTPUT`; assertions unchanged; perf harness gained the localhost guard; multiplayer socket check accepts
  `ws://` for an `http://` API. Documented in `docs/brand/qa/README.md`.
- `scripts/qa/brand-surfaces.mjs` + `locators.json`: six viewports, 200% zoom, reduced motion, role/label locators.
- `scripts/qa/route-transfer.mjs`: per-route compressed transfer against `vite preview`.
- Baseline at `f8c7ad3` under `docs/brand/qa/baseline/` (refinement UI, character customizer, bundle/transfer; surfaces,
  performance and local multiplayer results are appended as they complete — see `RESULTS.md`).

## In progress

- See the "Evidence" section of `docs/brand/qa/baseline/RESULTS.md` for the latest run status.

## Blocked

- Nothing hard-blocked. Hosted multiplayer (staging Vercel + staging worker) is not exercised from this lane; the local
  two-client pair replaces it for guest rooms.

## Requests for lead

- Port 5198 (W8's assigned Vite port) was occupied by a Codex-spawned `python -m http.server 5198` (pid 29339, parent is
  the Codex app-server) when the baseline started; 5210 likewise. W8 used 5208/5209/5211 instead. If ports are allocated
  to lanes, Codex's static servers should stay off the 519x/521x range, or lanes should always pass `UI_ORIGIN`.
- Wave 0 entry intents (`classroom=join|signin|teacher`) are wired as `pending` surfaces in `brand-surfaces.mjs`; ping W8
  when they land so `INCLUDE_PENDING=1` can be switched on.
- Pre-brand defects found by the matrix are listed in `docs/brand/qa/baseline/RESULTS.md` with owner lanes; none require
  a W8 change.

## Requests for Codex

- None yet. The local worker (`wrangler dev`, no secrets) serves guest rooms, so no hosted configuration is needed for
  the guest two-client procedure.

## Evidence paths

- `docs/brand/qa/README.md` — invocations.
- `docs/brand/qa/ACCEPTANCE-MATRIX.md` — board/extra-coverage matrix.
- `docs/brand/qa/baseline/` — results, screenshots, `BUNDLE.md`, `RESULTS.md`.
