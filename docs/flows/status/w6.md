# W6 — Editor chrome (command strip, camera cluster, header adoption, intent redirects)

Branch `claude/flows-w6` from `claude/flows-v2` at `3e0a609`; `claude/flows-v2` (W2 shell, W1, W4, head swap) merged
in at `5716754` before the header commit. Dev port for this lane: 5246 (`npx vite --port 5246 --strictPort`).

## Commits (in order)

1. `66afdf5` **Command strip** — `src/brick/CommandStrip.tsx` + `command-strip.css`. One floating panel bottom-centre
   replacing the desktop Inspector, the touch selection bar, the placement pill/panel, the shortcut hint bubble and the
   empty-state guide. States: idle ("Pick a brick from the drawer" + click/box-select hint, hidden on coarse pointers),
   brush loaded (Placing / Moving / Duplicating chip, "Click to place · Esc puts the brick down", Rotate, Raise/Lower,
   Cancel, Place), selected (count + name, "Drag to move · R rotate · Esc clears the selection", Rotate, Duplicate,
   Color popover with the twelve swatches + Any color → existing `CustomColorPicker`, Delete, Adjust → nudge/resize,
   Move, Copy, Paste, Focus, coordinates). 44px targets, inert under graphics pause, reduced motion drops the slide-in.
   The drawer palette is now **Brush color** and sets only the brush (`useBrushColor`); selection recolor (one undo
   entry, group semantics) lives in the strip's Color popover — `BrickStudioApp.test.tsx` covers both.
2. `71d964f` **Camera cluster + history** — `CameraCluster` (Frame, Top, Front, 3D; pressed state; inert under pause)
   as a column on the bottom-right edge; `HistoryCluster` (Undo, Redo, touch-only Box select, capacity) top-left beside
   the drawer. Old `.brick-edit-toolbar` / `.view-controls` / `camera-view-select` removed. Every shortcut and gesture
   unchanged.
3. `45995aa` **Header adoption** — `<AppHeader variant="editor">` from `src/shell` replaces the editor `Header`
   (props per W2's status note: `{...documentCommands}`, `onOpenSettings`, `onOpenHelp`, `worldTitle`, `onRenameWorld`,
   `saveStatus`, `onOpenWorldSetup`, `livePolicy`, `mode`, `onRequestMode`, `canExplore`, `exploreReason`,
   `onSaveToAccount` (guest drafts only), `onGoHome`). The editor renders `SettingsSheet` itself (Settings left the
   header; it is in the ⋯ menu). `?classroom=`: `save` opens the in-editor save sheet; the rest go through W2's
   `classroomIntentRedirect(intent, undefined, entrySearch)` (invite class codes ride to `/join`), param stripped.
   Placed-brick navigator moved into the strip's idle state as **Jump to brick** (`[`/`]` unchanged). `StudioMenu.tsx`
   now only exports `StudioDocumentCommands` (lead-owned `useBrickStudioDocuments` imports it). Old `.brick-header*`,
   `.studio-menu*`, `.studio-world-title` CSS removed; `--brick-header-height` stays 64 / 104 (≤700px) / 80 (≥1100px)
   / 56 (short landscape) as before; the compact-layout 60px override is gone so W2's two rows fit.
4. `f419230` **`/build?world=<id>`** — strips the param; signed out → `goToJoin({ mode: 'signin', next:
   '/build?world=<id>' })`; signed in → `browserClassroomClient.getWorld(id)` then `openCloudWorld(document, world)`,
   the same function the save sheet's Open uses (`cloud.attach` with its unsaved-draft guard; live room → active-world
   key + fresh `/build`). Unavailable id → current build kept, alert "That world isn't available" with **Back to My
   worlds** (`/worlds`) and **Keep building**. Three cases in `studioNavigation.test.tsx`.
5. (this commit) status note + screenshots.

## Stubs

None left. A local `src/shell/AppHeader.tsx` stub existed only in a WIP commit that was dropped when `claude/flows-v2`
(with W2's real shell) was merged; the final history is commit 1 → commit 2 → merge → commits 3–4.

## Verification

- `npx tsc -p tsconfig.app.json --noEmit`: clean for `src/brick/**` and `src/shell/**`. **Upstream:** `claude/flows-v2`
  itself fails the app typecheck in `src/pages/class/demoData.ts`, `src/pages/join/JoinPage.test.tsx`,
  `src/pages/worlds/worldsFixtures.ts` (fixtures missing `studentsCanShare`, `buildingNow`, `teacherName`,
  `classCanEdit`, `ownerClassId` after W1's contract change). Not W6 files; left for W4/W5/lead.
- Full `npx vitest run`: 121 files, 1231 tests green (after merge).
- Screenshots `docs/flows/qa/w6/{brush,idle,selected,selected-color,selected-adjust}-{1366x768,1024x768,390x844}.png`
  (Playwright, guest draft, onboarding dismissed; 390 uses touch emulation).

## Places the lead / other lanes must touch

- **W7 QA scripts** (`scripts/qa/**`, not W6 files) reference removed chrome: `scripts/qa/verify-desktop-space.mjs`
  (`.desktop-selection-panel`, `combobox 'Camera view'`, `button 'Frame Build'`, `.brick-edit-toolbar`),
  `scripts/qa/verify-touch-layout.mjs` (`.brick-edit-toolbar`, `menuitem /Settings/` now under the **This build**
  button, `Settings` button only in the Explore HUD), `scripts/qa/locators.json` (`worldMenu` "World menu" → button
  "This build"; `studioMenu` "Studio actions" → menu "This build"; `menuHome`/`menuMyWorlds`/`menuMyClass` no longer
  exist in the ⋯ menu — My worlds / My class are `href` items in the **Account** menu; `settings` is a menuitem in the
  build header and a button in Explore). New locators: `group 'Camera view'` with buttons `Frame build`, `Top view`,
  `Front view`, `3D view`; `data-testid="command-strip"`; `group 'Positioned brick actions'` / `'Selected brick
  actions'` / `'<n> bricks selected'`; `dialog 'Brick color'` / `'Color all <n> bricks'`; `note 'Build hint'`;
  `combobox 'Jump to brick'`; `group 'Brush color'` in the drawer.
- `scripts/qa/verify-refinement-ui.mjs` clicks `button 'Settings'` on the build header — now `This build` → `Settings`.
- The lead's `?classroom=` contract line ("`if (intent !== 'save' && classroomIntentRedirect(intent)) return`") is
  implemented with the original `entrySearch` passed explicitly because the editor strips the query before the
  redirect helper would otherwise read `window.location.search`.
- No `,` shortcut existed for Settings, so none was added (the ⋯ menu is keyboard-reachable: This build → Settings).
- Explore HUD (`ExploreHud`) is unchanged and still shows `StudioSettings` (W2 note 5: it may adopt `ModeSwitch` /
  `AccountChip` later).

## Requests for lead

- Merge order: this branch already contains `claude/flows-v2` up to `c74c125`; a fast-forward or merge of
  `claude/flows-w6` into `claude/flows-v2` should be conflict-free.
- Fix or assign the upstream `src/pages` fixture typecheck errors listed above before `npm run check`.
