# W2 — Marketing landing (boards 01, 02, 16 landing panels)

Branch `claude/brand-w2` (base `7d9a48b`, W0 + W1 foundation). Dev port 5192. Ownership: `src/brick/landing/**`,
`docs/brand/copy/**`.

## Done

- `src/brick/landing/LandingPage.tsx`: Brickgineers landing on the shared foundation. `BRAND_NAME` + `BrandLockup` from
  `src/brand` (header 36 px, footer 24 px); CTAs are links carrying the `src/ui` button classes
  (`ui-button ui-button-{primary,secondary} ui-button-lg`); the mobile menu toggle is the real `Button`.
  Copy per plan §4: hero "Build a world. / Then step inside.", "Create, explore, and build together in your browser.",
  Start building / Continue building (only when `brick-studio.current-project.v1` exists; read-only, never created),
  Join a class → `/build?classroom=join`, header How it works / For teachers / Sign in (`?classroom=signin`),
  Teacher sign in (`?classroom=teacher`, Google primary, "this page does not create new teacher accounts"),
  "No account needed to start.", steps Build it → Explore it → Bring friends, the explain line, guest vs class account
  cards, classroom band (shared class worlds available only while the teacher keeps collaboration open; no "join
  anytime"), teachers section with verified controls, three factual FAQs, in-page Help and Privacy cards as the footer
  targets. Every claim was fact-checked against the code (see `docs/brand/copy/*.md`); corrected two inherited
  inaccuracies (there is no "lock", the control is "Close room to new people"; the file menu items are Export/Import,
  so the copy no longer names them).
- Board 16: phone hero order headline → art → Start building → Join a class → Sign in → reassurance (`grid-template-areas`,
  hero Sign in link shown ≤ 760 px only).
- Media: `<picture>` with avif + webp + png sources on the exact W7 paths/intrinsic sizes
  (`hero-{800,1200,1600}` 1600×960, `scene-*-{400,800}` 800×500, `character-nova-400` 400×400). The SVG art is always
  painted underneath; the raster fades in on load (cached images handled) and a missing file leaves the art in place —
  no code change needed when W7's files land. Hero eager/high priority, everything below the fold lazy.
- Deep links: the page is a lazy chunk, so `/#teachers` etc. are resolved after mount and again after
  `document.fonts.ready` (font swap shifted the target by ~17 px).
- `landing.css`: no `--landing-*` definitions remain (test-enforced); every color/radius/shadow/font is a `:root`
  semantic token (`--primary-soft`, `--warning-soft`, `--brand-butter` for the butter accents with ink text). Focus
  outline 3 px. Reduced motion: no drift/twinkle, no CTA transforms, no media fade, instant scroll.
- `LandingArt.tsx`: `PlaceholderMark` removed (the real `BrickMark` is used).
- Tests `LandingPage.test.tsx`: 21 tests incl. the import boundaries (no three/store/editor imports, no `@import`, no
  `https:`), avif/webp/png sources, always-present fallback, no `--landing-*`, brand lockup + ui-button classes, hero
  Sign in order, Continue building without touching the draft, honest-copy regexes.
- `docs/brand/copy/help.md`, `docs/brand/copy/privacy.md`: the shipped copy with source references and open questions.
- `docs/brand/qa/w2/`: script, 14 screenshots, `report.json` (42/42 checks), README.

## In progress

- Nothing.

## Blocked

- Nothing. W7 media files are optional at runtime (vector fallback); the media byte budget can only be measured once
  they exist.

## Requests for lead

1. `src/ui/Button.tsx`: a link variant (e.g. `ButtonLink` / `as="a"` with `href`) would let the landing stop hand-composing
   `ui-button` classes on anchors (`CtaLink` in `LandingPage.tsx`). Not blocking.
2. `src/main.tsx` line 36: the Suspense fallback text still reads "Welcome to Brick Studio…"; suggest
   `Welcome to ${BRAND_NAME}…` from `src/brand`.
3. Decision: should Help and Privacy become routes (`/help`, `/privacy`) instead of `/#help` / `/#privacy`? The copy is
   ready either way; routes need `src/routes.ts` (lead) and a real privacy review (open questions in
   `docs/brand/copy/privacy.md`).
4. W4: once the menu items become "Download build" / "Import build" per the header contract, the Help copy in
   `LandingPage.tsx` and `docs/brand/copy/help.md` may name them explicitly.

## Evidence

- `npx tsc -p tsconfig.app.json --noEmit`: clean.
- `npx vitest run src/brick/landing`: 1 file, 21 tests passed. Full `npx vitest run`: 104 files, 968 tests passed.
- Browser: `docs/brand/qa/w2/report.json` (42 checks ok, no page errors), screenshots listed in `docs/brand/qa/w2/README.md`.
- A Vite dev server started by this lane may still be listening on 127.0.0.1:5192 (log in the session scratchpad).
