# Classroom polish candidate — local, not deployed

Worktree: `/Users/ahmaadidrees/.codex/worktrees/brick-classroom-release`, branch `codex/classroom-polish-today`. Initial polish checkpoint `98e5cad` is pushed. The guest Build together release is integrated into this candidate so future classroom polish retains account-free room creation and invitation links. The public release is documented separately in GUEST-HOTFIX-2026-09-10.md.

## Implemented

- Compact signup with visible primary action, password reveal, minimum-length guidance, enrollment/returning-code distinction, and teacher-reset help.
- Teacher Students / Shared worlds / Class settings sections, roster search, focused management forms, clear world cards, and revision numbers confined to recovery controls.
- Sign out now respects a failed pending-world save instead of clearing the active account and replacing the editor state.
- Stable My Worlds navigation, separate cloud/browser-only status and current cloud-world title, and visible Scene & character customization. Builder shortcuts pause while customization is open.
- Consistent Scene wording, friendly character descriptions, live SVG color previews, default-color reset, accessible tabs, and landing copy reflecting both guest rooms and optional classroom accounts.
- Avatar poses update a dedicated subscription rather than rerendering the editor. Thumbnail GPU/PNG generation is visible-first, scheduled across frames, and bounded by an LRU cache. Recovery records serialize one document rather than a stale and current copy.
- Cloud-open toast no longer incorrectly describes every restored document as a local save.

## Evidence before guest integration

- Initial full Node22 check:674 frontend+61Worker tests, types and build passed.
- Browser: new student signup → My Worlds save → matching API and authoritative PostgreSQL document → signout → independent fresh-browser login/open preserved the exact Toy Room build.
- Signup primary action at1280×633 moved from y672–715 below the viewport to y516–556;390×844 action y708–748, no horizontal overflow. Teacher sections reviewed at both sizes; long management forms intentionally scroll.
- Rendering test:60 separate pose updates produced60avatar renders and zero extra editor renders; WebGL editor mocked, actual session/subscription path exercised.
- Chromium thumbnail sample at1280×633:8of23mounted cards initially rendered,15deferred. A color change generated8PNGs spaced15.9–16.8ms; scrolling rendered5newlyvisible cards; returning to a cached color encoded zeroPNGs. These are Mac/headless browser observations, not Chromebook FPS.

## Remaining release work

The integrated guest/classroom candidate passes the full Node22 check:679frontend+64Worker tests, types and build. Both guest and classroom rendering-isolation regressions pass. Choose the next production release window after final browser review. Actual student/Chromebook observations remain required. Larger world-library visual previews, account-synced character preferences, Explore brick instancing, and broader graphics-quality controls were not implemented in this pass.

Source-review follow-up: geometry and existing custom-thumbnail caches key by part ID; investigate imports that reuse a custom ID with changed geometry. This pre-existing concern was not changed by thumbnail scheduling.
