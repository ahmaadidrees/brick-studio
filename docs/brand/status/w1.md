# W1 — Brand foundation and design system

Branch `claude/brand-w1` (base `6950d4b`, the integration branch with W0/W1/W2/W3/W5/W6/W7/W8). Dev port 5191.

## Done

First pass (merged):

- `src/brand/**`: `BRAND_NAME`, `BRAND_TAGLINE`, `BRAND_DESCRIPTION`, `BRAND_SHORT_DESCRIPTION`, `BRAND_PALETTE`,
  `BRAND_PRIMARY_STRONG`, `BRAND_MARK_COLORS`, font stacks, `BrickMark`, `Wordmark`, `BrandLockup`, `brand.css`.
- `src/styles.css`: semantic tokens per CONTRACTS.md plus the legacy aliases; global focus ring, reduced-motion kill
  switch, base resets. `src/ui/`: `Button`, `Field`/`TextField`, `Sheet`/`Dialog`, `SegmentedControl`, `SaveStatus`,
  `Gallery` (`/dev/ui`), tests. Identity assets and `index.html` copy. Evidence under `docs/brand/qa/w1/`.

Second pass (this lane, commit `203d827` + evidence commit):

- `Button`: `href` renders an `<a>` with the same classes, sizes and focus ring; `disabled`/`loading` links drop the
  href and set `aria-disabled` (`role="link"` kept). Typed `ButtonLink` exported. `pressed` prop sets `aria-pressed`
  and the styled `.ui-button-pressed` state (per variant). Consumers passing `aria-pressed` directly are unchanged.
- `Field`/`TextField`: the required "*" now sits outside the `<label>` (aria-hidden) so the accessible name stays the
  plain label; `requiredMark={false}` hides it. `getByLabelText('Password')` works with `required`.
- `Select` (new): Field + native `<select class="ui-input ui-select">` + chevron; `required`, `error`, `hint`,
  `disabled`, forwardRef to the select.
- `SegmentedControl`: `aria-describedby` passthrough on the radiogroup.
- `ui.css`: under `(pointer: coarse)` `.ui-button-sm` (and icon-only sm) and every `.ui-segmented-option` reach
  `--control-height` (44px). Anchors get pointer cursor; `[aria-disabled="true"]` shares the disabled look and blocks
  hover states.
- `Sheet`: never records `<body>` as the restore target (an opener unmounting in the same commit left focus on body);
  passive re-check after open reclaims focus stolen by a sibling's cleanup and remembers what took it as the close-time
  fallback; skips restoration when another modal dialog already holds focus.
- Gallery: link buttons, pressed toggles, Select, described segmented group; unit tests for each addition
  (`src/ui`: 10 files / 63 tests).
- Evidence: `docs/brand/qa/w1/sheet-scroll.mjs` (Sheet body scrolls under the persistent footer at 844×390 and
  683×384; 44px targets on touch; new examples at both viewports) and the consistency review
  `docs/brand/qa/w1/CONSISTENCY-REVIEW.md` (+ `consistency-review.mjs`, `review/findings.json`, 13 screenshots).
- Scratch `dev-gallery.html` / `dev-probe.html` removed (the `/dev/ui` route replaced them).
- Identity assets not regenerated: no mark or token changed.

## In progress

- Nothing.

## Blocked

- Nothing.

## Requests for lead

Consumer changes recommended (all optional, backward compatible; details and line numbers in
`docs/brand/qa/w1/CONSISTENCY-REVIEW.md`):

1. **W2** `LandingPage.tsx` `CtaLink` → `<Button href …>` / `ButtonLink`.
2. **W3** delete `classroom.css:182–183` (now done by the primitive); replace `fields.tsx` `TextInput`/`SelectInput`
   with `TextField`/`Select` and drop `.classroom-select*`; `TeacherGoogleCallback` anchors → `Button href`;
   `.classroom-recovery button` → `Button`.
3. **W5** `WorldAndCharacterSheet.tsx` plate size → `SegmentedControl` with `aria-describedby` (both blockers gone);
   the `CreateBrickSheet.tsx:106–126` focus workaround can go once W4's drawer lands (Sheet handles it now).
4. **W6** delete `character-studio.css:244` (`.character-studio__lock { min-height: 36px }` under coarse — the only
   sub-44px controls left in the sheet on touch); switch `aria-pressed={…}` to `pressed={…}` and drop the local
   `[aria-pressed]` rules where the shared look is enough.
5. **W4 / lead** (editor shell, pre-brand on this branch): `brick-studio.css:2–10` legacy `--studio-*` block blocks the
   palette; remove `backdrop-filter: blur()` from the panels over the canvas; `system-ui` on `.brick-save-status` →
   `SaveStatus`; 9–10px greys under 4.5:1 → `--text-muted` ≥ 12px; radii → tokens; World menu 23px tall;
   `worldTitle || 'Brick Studio'`, `StudioMenu.tsx` "Brick Studio" strings → `BRAND_NAME`. Same pattern in
   `live-world.css` (`--live-*`, eight blurs) and `explore-camera-settings.css` (settings modal not on `Dialog`).
6. **Lead** `src/main.tsx:36` fallback copy → `BRAND_NAME` (W2 noted it too).
7. Optional tokens `--primary-hover` / `--danger-hover` for the literal hover shades in `ui.css` if a lane needs them.

## Evidence

- `npx tsc -p tsconfig.app.json --noEmit`: clean. `npx vitest run src/ui src/brand src/test`: 10 files / 63 tests.
  Full `npx vitest run`: 109 files / 1073 tests green (after the primitive commit; later commits are docs/evidence only).
- `docs/brand/qa/w1/README.md` (scripts and checks), `docs/brand/qa/w1/CONSISTENCY-REVIEW.md`,
  `docs/brand/qa/w1/review/findings.json`, PNGs listed in both.
