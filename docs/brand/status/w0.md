# W0 — lead-delegated contract wiring

Worktree `/Users/ahmaadidrees/.codex/worktrees/brand-w0`, branch `claude/brand-w0`, base `f8c7ad3`.
Structure-only refactors; nothing restyled. Node 22, `npm test` 97 files / 920 tests green,
`npx tsc -p tsconfig.app.json --noEmit` and `npm run typecheck:brick-core` clean at every commit.

## Done

1. **Entry-intent contract** — `2f77019`
   - `src/routes.ts`: `ClassroomEntryIntent`, `isClassroomEntryIntent`, `parseClassroomEntryIntent(search)`.
   - `src/brick/BrickStudioApp.tsx`: uses the shared parser; `?classroom=` is stripped from the address bar
     for every value, including unknown ones (previously unknown values were left in place — behavior fix
     requested by the lead).
   - `src/classroom/ClassroomPanel.tsx`: `intent: ClassroomEntryIntent`; `join` → register, `signin` → login,
     `teacher` → teacher-login as the initial guest mode; `worlds`/`class` → tab, `save` → save flow as before.
     Signed-in users see My Worlds for `join`/`signin`/`teacher`. Google round-trip sends
     `/build?classroom=<intent>` for all six values (tested).
   - Tests: `src/routes.test.ts`, `src/classroom/ClassroomPanel.test.tsx` (mode per intent, signed-in
     fallback, round-trip ×6), `src/brick/studioNavigation.test.tsx` (`/build?classroom=bogus` opens no panel and
     strips the param; known intents open the panel and keep other params/hash).
2. **CharacterStudio extraction** — `e9a09d8`
   - New `src/brick/characters/CharacterStudio.tsx` (+ `CharacterStudio.test.tsx`), exported from
     `src/brick/characters/index.ts`. Props exactly as contracted: `draft, onDraftChange, characterDescriptors,
     paletteGroups, previewStatuses?, onRequestPreview?`. Owns preview, character cards (ContentPicker with
     `visibleSection="character"`), the toy-figure `AppearanceControls` special case, `LookColors`, `WardrobePanel`.
   - `WorldAndCharacterSheet.tsx` keeps frame/tabs/plate controls and renders the scene picker on its tab and
     `<CharacterStudio/>` on the other. DOM order and markup are unchanged; all contentPicker/characters tests pass
     without edits.
   - Import direction: characters → contentPicker/ContentPicker (runtime) + contentPicker/selection (type-only).
     No module cycle (ContentPicker only type-imports `characters/types`).
3. **Live state views** — `218b9b5`
   - New `src/brick/live/LiveStateViews.tsx`: `BlockedView`, `OpeningRoomView`, `ClassroomAccessChangedView`
     moved verbatim, plus the `friendlyReason` / `exportLiveWorldCopy` helpers they call (exported; LiveWorldPage
     imports them back). No logic change; LiveWorldPage tests green.
4. **Dev UI gallery route** — `ec388a3`
   - `resolveAppRoute(location, { dev = import.meta.env.DEV })` returns `'dev-ui'` for `/dev/ui` only in dev;
     `/dev/ui` is `not-found` in production. `src/main.tsx` lazy-loads `./ui/Gallery` behind the same DEV guard;
     a scratch `vite build` emitted no gallery chunk. `src/ui/Gallery.tsx` placeholder (default export, "UI gallery").

## In progress

Nothing.

## Blocked

Nothing.

## Requests for lead

- `src/brick/live/LiveStateViews.tsx` also hosts `friendlyReason`/`exportLiveWorldCopy` (moved to avoid a
  LiveWorldPage ↔ views cycle). If W4 should own only markup, consider moving those two helpers into a
  lead-owned module later; behavior is identical either way.
- `CharacterStudio` renders the character card grid through `ContentPicker` (W5 file). W6 restyling the cards
  will need a W5 request; the split matches CONTRACTS ("W6 owns everything inside CharacterStudio").
- `ResolveAppRouteOptions.dev` is a test seam only; `main.tsx` never passes it.
