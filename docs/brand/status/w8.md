# W8 — Independent QA and evidence

Worktree `/Users/ahmaadidrees/.codex/worktrees/brand-w8`, branch `claude/brand-w8`. Owns `scripts/qa/**`, `docs/brand/qa/**`
(not the lane subfolders w1–w7), this file. No product files touched; the branch merged `claude/brickgineers-brand`
(`907a06c`) at `acec80e` so the transfer build and W8's worker-pointed dev server match the candidate
(`git diff --stat 907a06c HEAD -- src public` is empty).

## Done — integrated-candidate run (2026-09-14, candidate `907a06c`)

Full record: **`docs/brand/qa/integrated/RESULTS.md`** (exact SHA and timing note, every command with counts, per-board
table, D1–D10 re-measured, new defects N1–N8 with owner/file/selector/viewport/severity, evidence gaps).

- Locators (`scripts/qa/locators.json`) updated to the final accessible names, each verified in the DOM at 5190:
  `Brickgineers Home`, Scene / Character / Settings, `Explore mode`, `Build together` ↔ `People, N here`, `World menu`,
  save chip by `[role="status"][data-kind]` (new `css` locator type), classroom radios Student / Join a class / Teacher
  (`aria-checked`), dialog titles Join your class / Welcome back / Teacher sign in / My Worlds / My Class / Choose a new
  password, `Class sign-in code`, `Close and keep building`, mobile brick sheet `Bricks`, `Choose Brickgineers project
  file`, `Download build`, published region + `Make a copy` (compact bar: long aria-label, reported as N6), live gate
  `Your name` / `World name` / `Create shared world` / `Join world`, People panel `Invite link` / `Copy link` / `Export
  copy` / `Close room to new people`; character radios `Toy Figure.…` and Idle / Walk / Run / Jump / `Pause character
  animation` unchanged.
- Harness fixes needed by the new UI: topmost-dialog focus rule (color picker stacks above the brick sheet), sheet-scoped
  selected-tab checks (the drawer has a selected category tab), teacher entry split into Google-first and email states,
  refinement-UI collects per-viewport failures and settles the Settings sheet, two-client harness/board 11 follow the
  People panel, board 02 scrolls to the final landing headings.
- Results at `907a06c`: vitest `BrickStudioApp.test.tsx` ×3 = 54/54 each (the flaky test did not reproduce, host load
  11–22); strict surfaces matrix 275 runs = 194 passed / 81 failed / 8 notes (81 = N1 ×62, N6 ×8 → 22/22 on re-run, N2 ×3,
  N3, N4, N5 ×2); refinement UI 4/7 (three touch viewports fail only on N1); character customizer passed (D8 fixed, 6/6
  GLB); schema round trip 3/3; boards 61 entries = 51 captured / 10 need a classroom or teacher fixture / 0 failed
  (PNG at 1366×768 + 390×844, board 11 in a real local room); local two-client room 9/9 (export == authoritative GET,
  cold rejoin equal, no owner fragment in the invite, `characterShortcutTab` = Character); route transfer 5/5 — landing
  151.7 KB (+73 % vs 87.7 KB: +59 KB brand media within the 250 KB hero / 600 KB initial budgets, +5.3 KB JS, no editor
  chunk), every other route within +2.3 %; expanded performance on a quiet host (load 4.5 → 4.2): Toy Room 87 draw
  calls / 923,174 tris / RAF p95 16.7 ms (pre-brand 103 / 923,622 / 16.7), Sky Island and Brick Valley identical to
  pre-brand, travel beyond the plate edge in all three.
- D1–D5, D8, D10 fixed; D6 was a harness false positive (child dialog holds focus by design); D7 mostly fixed (rest = N2,
  N4); D9 replaced by an explicit "This world needs a class sign-in" state.

## Blockers for the candidate

- None. Every guest flow works end to end; all eight new defects are non-blockers (details and suggested one-line fixes
  in RESULTS.md). Highest priority: **N8** (People panel content clipped at 1366 and 390 — grid blowout in
  `.live-panel-section`, `live-world.css:205`) and **N1** (World menu 44 px hit box starts 8–12 px above the viewport on
  every touch viewport — `brick-studio.css:408` negative margin; 62 strict-matrix rows).

## Requests for lead (defects to route)

| # | Lane | One-line ask |
|---|---|---|
| N1 | W4 | `brick-studio.css:408`: replace `margin: -8px 0` on `.studio-world-title` (pointer: coarse) with a `::before` hit area or let the header row grow |
| N2 | W5 | `BrickStudioApp.tsx:498` `Search bricks` input: 44 px min-height on touch layouts |
| N3 | W4 | `brick-studio.css:106` `.studio-menu-popover`: `max-width: calc(100vw - 16px)` (overflows at 320) |
| N4 | W4 | `brick-studio.css:319` `.brick-explore-cluster .brick-header-tool`: 44 px min-height on touch (currently 40) |
| N5 | W5 / W1 | 200 % zoom at 1024×768: opening the Bricks sheet scrolls the header out of view (Sheet open → scroll position) |
| N6 | W4 | `BrickStudioApp.tsx:390` published copy button: keep one accessible name (`aria-label="Make a copy"`) when the label is hidden |
| N7 | W3 / W1 | classroom sheet at 390: page scrolls 8 px on open (re-check after N1) |
| N8 | W4 | `live-world.css:205` `.live-panel-section { grid-template-columns: minmax(0, 1fr); min-width: 0 }` |

- Boards 04 (reset) / 05 / 13 / 14 still need a session fixture (`SESSION_STORAGE_FILE`) or a staging pair with the
  classroom secrets; W8 will run `BOARDS=04,05,13,14` on request.
- Suggest recording `docs/brand/qa/integrated/expanded-performance/results.json` as the reference row for the next
  candidate (the pre-brand baseline has no numbers).

## Evidence paths

- `docs/brand/qa/integrated/RESULTS.md` — the report; `docs/brand/qa/README.md` — run book (integrated-run layout added).
- `docs/brand/qa/integrated/{surfaces,surfaces-evidence,surfaces-defects,surfaces-published-rerun,refinement-ui,
  character-customizer,schema-roundtrip,boards,multiplayer-local,route-transfer,expanded-performance}/` (≈ 19 MB;
  boards are PNG, surfaces JPEG).
- Scratch (not in the repo): probe scripts, per-harness logs and the three vitest logs under the session scratchpad.
